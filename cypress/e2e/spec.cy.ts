describe('My First Test', () => {
  it('Visits the initial project page', () => {
    cy.visit('/user')
    // cy.get('button[matTooltip="Add user"]').click();
    // cy.get('input[placeholder="First Name"]').type('Peter');
    // cy.get('input[placeholder="First Name"]').type('Peter');
    // cy.get('input[placeholder="Last Name"]').type('Müller');
    // cy.get('input[placeholder="E-Mail"]').type('john.doe@example.com');
    // cy.get('input[placeholder="MM/DD/YYYY"]').type('01/01/1990');
    // cy.get('input[placeholder="Street + House No."]').type('123 Main St');
    // cy.get('input[placeholder="Zip Code"]').type('12345');
    // cy.get('input[placeholder="City"]').type('Köln');
    // cy.get('button').contains('Save').click();
    // cy.get('mat-card-content table tr').contains('Müller').click();
  })
})
